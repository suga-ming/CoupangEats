import React from "react";
import styled from "styled-components";

const Container = styled.div`
  margin-top: 54px;
  margin-bottom: 74px;
`;

const Heart = () => {
  return <Container>Heart</Container>;
};

export default Heart;
